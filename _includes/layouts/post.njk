---
layout: layouts/base.njk
---
{# Этот комментарий можно удалить.
   Этот шаблон предельно прост. Он берет за основу наш главный скелет `base.njk`
   и вставляет в него контент статьи, обернутый в тег <article> с нашим новым
   CSS-классом `post-content` для красивого форматирования текста.
#}
<article class="post-content">
	<h1>{{ title }}</h1>
	<time datetime="{{ page.date | htmlDateString }}">{{ page.date | readableDate }}</time>
	{{ content | safe }}
</article>
